import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RutaplaneadaPage } from './rutaplaneada.page';

describe('RutaplaneadaPage', () => {
  let component: RutaplaneadaPage;
  let fixture: ComponentFixture<RutaplaneadaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RutaplaneadaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
